{
  "name": "photospace-runtime",
  "version": "0.3.0",
  "description": "Renderer-agnostic Photospace runtime for depth-aware creative web assets.",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/atsekkei/photospace.git",
    "directory": "packages/runtime"
  },
  "keywords": ["photo-space", "depth", "webgl", "three.js"],
  "main": "./dist/loader.js",
  "module": "./dist/loader.js",
  "types": "./dist/loader.d.ts",
  "exports": {
    ".": {
      "types": "./dist/loader.d.ts",
      "import": "./dist/loader.js"
    }
  },
  "files": [
    "dist"
  ],
  "sideEffects": false,
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsup loader.ts --format esm --out-dir dist --clean && tsc -p tsconfig.json --outDir dist",
    "prepublishOnly": "pnpm build"
  },
  "devDependencies": {
    "tsup": "^8.5.1"
  }
}
